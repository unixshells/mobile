import 'dart:ui';

const relayHost = 'unixshells.com';
const relayJumpHost = 'relay.unixshells.com';
const relaySSHPort = 22;
const apiBaseURL = 'https://unixshells.com';
const defaultSSHPort = 22;
const terminalType = 'xterm-256color';

// Theme colors — matches web terminal dashboard.
const bgDark = Color(0xFF0b0c10);
const bgCard = Color(0xFF12141a);
const bgSurface = Color(0xFF1a1c24);
const bgSidebar = Color(0xFF0f1117);
const accent = Color(0xFF6aaa6a);
const accentDim = Color(0x146aaa6a);
const textBright = Color(0xFFe2e6ec);
const textDim = Color(0xFF7c8594);
const textMuted = Color(0xFF4a5060);
const borderColor = Color(0x0FFFFFFF);

// Legacy aliases.
const bgButton = bgSurface;
